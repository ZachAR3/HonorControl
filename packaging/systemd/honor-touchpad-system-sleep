#!/bin/sh
# Re-run the typed profile transaction after the HID controller resumes.

if [ "$1" = "post" ] && \
   systemctl is-enabled --quiet honor-touchpad-restore.service; then
    systemctl --no-block restart honor-touchpad-restore.service
fi
